spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://localhost:3307/awesomepizza?allowPublicKeyRetrieval=true&useSSL=false
    username: pizza
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  servlet:
    context-path: /api